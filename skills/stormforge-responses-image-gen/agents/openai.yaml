display_name: StormForge Responses Image Gen
short_description: Generate and edit high-quality images via Responses API relays with Base64 references.
default_prompt: Build a concise visual design brief, generate or edit the requested image with the Responses API image_generation tool, and save the image plus non-sensitive metadata.
